# 🚗 Car Price Prediction App

This is a machine learning web application built using **PyTorch** and **Streamlit**.

## Features
- Predicts car price based on:
  - Car age
  - Mileage
  - Accident history
- Model trained with normalized features
- Clean Streamlit UI
- CPU-only inference

## Tech Stack
- Python
- PyTorch
- Streamlit

## How to Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
